machine:
    node:
        version: 4.2.1
test:
  override:
      - cd client && npm test && cd ../server && npm test && cd ..
dependencies:
  override:
    - cd client && npm install && cd ../server && npm install && cd ..
  pre:
    - sudo apt-get install build-essential
    - sudo apt-get install gcc
    - sudo apt-get install g++